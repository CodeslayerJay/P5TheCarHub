@model VehicleViewModel
@{
    Layout = "~/Views/Shared/_ManageLayout.cshtml";
    
}

<div class="pt-5">
    <h4 class="text-center">Vehicle Details</h4>

    <div class="row pt-1">
        <div class="col-10 m-auto">
            

            <ul class="nav nav-tabs" id="myTab" role="tablist">
                <li class="nav-item">
                    <a class="nav-link active" id="main-tab" data-toggle="tab" href="#main" role="tab" aria-controls="main" aria-selected="true">Vehicle</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="repairs-tab" data-toggle="tab" href="#repairs" role="tab" aria-controls="repairs" aria-selected="false">Repairs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="invoice-tab" data-toggle="tab" href="#invoice" role="tab" aria-controls="invoice" aria-selected="false">Invoice</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" id="photos-tab" data-toggle="tab" href="#photos" role="tab" aria-controls="photos" aria-selected="false">Photos</a>
                </li>
            </ul>
            <div class="tab-content card p-3" id="myTabContent">
                <div class="tab-pane fade show active" id="main" role="tabpanel" aria-labelledby="main-tab">
                    <partial name="DetailPartials/_VehicleInfoPartial" />
                </div>

                @*<!-- REPAIR INFORMATION -->
                <div class="tab-pane fade" id="repairs" role="tabpanel" aria-labelledby="repairs-tab">
                    <partial name="DetailPartials/_RepairInfoPartial" />
                </div>

                <!-- INVOICE INFORMATION -->
                <div class="tab-pane fade" id="invoice" role="tabpanel" aria-labelledby="invoice-tab">
                    <partial name="DetailPartials/_InvoiceInfoPartial" />
                </div>

                <!-- PHOTOS -->
                <div class="tab-pane fade" id="photos" role="tabpanel" aria-labelledby="photos-tab">
                    <partial name="DetailPartials/_PhotosPartial" />
                </div>*@

            </div>
        </div>
    </div>
</div>
